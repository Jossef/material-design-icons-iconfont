name: CI

on:
  schedule:
    - cron: "* * * * *"
    # - cron: "0 0 * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '12'
      - run: npm install
      - run: npm run check-for-font-updates
      - run: echo "an update is available TODO - push a commit with changes"
